excel 函数：
  函数可以分为5大类：
  1. 文本清洗类
  2. 关联匹配类
  3. 逻辑运算类
  4. 计算统计类
  5. 时间序列类
  
  
  
1. 文本清洗类：
  常用的有：
  1.1 [find]  find (find_text,find_range,start_num)  在文本中找到对应的词的位置。
  1.2 [substitue]  SUBSTITUTE(text,old_text,new_text,[instance_num])   替换文本中的某些词。可以用于去除内容中间的空格。
  1.3 [left,right,mid] 从左边，右边 截取多少个字符，其中mid  是定位到中间，从第几位开始返回几个数字
  1.4 [text]   改变数字，时间的格式。
  1.5 [concatenate]  连接文本字符串
  1.6 [trim]  去除空格，去除的是内容前后的空格，而不是内容里面的。
  1.7 [replace]  Replace(old_text,start_num,num_chars，new_text) 
  1.8 [len]  取字符串长度
2. 关联匹配类：
  常用的有：
  2.1 [lookup] 查找值。需要注意查找值所在列升序排序
  2.2 [Row]  返回单元格所在的行
  2.3 [Vlookup] 同样是查找值，只能按列查找，同时查找值所在范围需要是第一列。
  2.4 [column] 返回单元格所在列
  2.5 [Index] 返回单元格区域内的 某行某列的内容
  2.6 [Offset]  以某个单元格或者某个区域为目标，引用对应偏移量的某单元格或者某区域的内容。
  2.7 [Match] MATCH(lookup_value, lookup_array, [match_type])  在某区域内所搜某值，返回该值在单元格区域内的相对位置 注意于lookup函数的区别。
  2.8 [Hyperlink] 做外部跳转连接的。
3. 逻辑运算类：
  and,not,or ,false true, if ,is 等。
4. 计算统计类：
  sum, count,max, min ,avgragea，stdev 求和，计数，最大值，最小值，平均值,样本偏差等。
  4.1 【Rank】  rank(number,ref,[order]) 某值在某列的排名。
  4.2 【sumproduct】  返回的是，行对应相乘并相加，类似于线性代数的行列运算。 同时可以限定条件，满足某些条件的行才做计算。
  4.3 【subtotal】   分类汇总计算，采用引用的方式，使用时需要百度确认。
5. 时间序列类：
  本质是数字
  year,day,month,date,weekday,now,weeknum,today等。
  

EXCEL 图表：
  1. 柱形图：用于作比较，横轴是时间轴，纵轴是数据轴，比较不用数据之间的差距，但是不能比较多个维度的数据。
  2. 折线图：看数据变化趋势，时间维度看数据变化的趋势，发现整体趋势或者是某些突出的点。可以多维度比较。
  3. 饼状图：用来看数据占比的。
  4. 散点图：主要是二维数据的比较，以及发现相关关系。
  5. 气泡图: 散点图加入大小后的三维比较。
  6. 雷达图：多个维度的比较。可以用于不同用户群体，在相同维度下的不同表现。
  
  
 数据透视表，实践。
